[phases.setup]
nixPkgs = ["nodejs_20", "python3", "python3Packages.pip", "ffmpeg"]

[phases.install]
cmds = [
  "npm ci",
  "python3 -m pip install --no-cache-dir yt-dlp"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
